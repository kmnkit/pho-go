# Test Results

このディレクトリには Playwright E2E テストの結果が保存されます。

## ファイル

- `summary.md` - テスト結果のサマリー（Git トラック対象）
- `results.json` - テスト結果の生データ（.gitignore で除外）

## 使い方

### テストを実行してサマリーを生成

```bash
# テストを実行
npm run test:e2e

# サマリーを生成
npm run test:e2e:summary

# または一度に実行
npm run test:e2e:full
```

### サマリーファイルの確認

```bash
# Markdown ファイルを確認
cat tmp/test-results/summary.md

# または任意のマークダウンビューアで開く
```

## サマリーに含まれる情報

- ✅ 合格したテスト数
- ❌ 失敗したテスト数
- ⚠️ Flaky なテスト（リトライで成功したもの）
- ⏭️ スキップされたテスト
- ⏱️ 実行時間
- 📝 合格率

### 失敗したテストの詳細

- テスト名
- ファイルパス
- エラーメッセージ
- タイムアウト情報

### 推奨事項

失敗やFlaky なテストに対する改善提案が自動的に生成されます。

## CI/CD での使用

GitHub Actions や Vercel では、このサマリーファイルをアーティファクトとして保存したり、
プルリクエストのコメントに追加したりできます。

### GitHub Actions の例

```yaml
- name: Run E2E tests
  run: npm run test:e2e:full

- name: Upload test summary
  uses: actions/upload-artifact@v4
  if: always()
  with:
    name: test-summary
    path: tmp/test-results/summary.md
```

## トラブルシューティング

### サマリーが生成されない

1. テストが実行されているか確認: `npm run test:e2e`
2. `tmp/test-results/results.json` が存在するか確認
3. スクリプトを手動で実行: `node scripts/generate-test-summary.js`

### 古いサマリーが残っている

```bash
# 古い結果をクリア
rm tmp/test-results/results.json
rm tmp/test-results/summary.md

# テストを再実行
npm run test:e2e:full
```
